# Copy this file to .env and add the key from Google AI Studio.
GEMINI_API_KEY=

# Optional: override the default Gemini model.
GEMINI_MODEL=gemini-3.5-flash

# Optional: used once when a retryable error keeps failing on the primary model.
GEMINI_FALLBACK_MODEL=gemini-3.5-flash
